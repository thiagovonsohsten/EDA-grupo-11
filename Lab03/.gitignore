# Nunca versionar credenciais nem state local
*credentials*
*.csv
.env
.aws/
.DS_Store
**/terraform/.terraform/
**/terraform/*.tfstate
**/terraform/*.tfstate.*
**/terraform/backend.hcl

# Pacote do professor (tem gabarito) — não entra na entrega
# (o pacote vem como .zip; o conteudo util foi extraido para a pasta aluno-*)
*.zip
*.pptx
